import {
} from '../src';

const samples: Array<any> = [
  {
    value: '',
    type: ''
  }
];

describe('Testing cloneDeep...', () => {
  it('Should do something', () => {
    const fn = (x) => x; 
    expect(fn({})).toEqual({});
  });
});

